# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local scrape cache & artifacts — regenerated by the build.
# (The recipe HTML cache is persisted between CI runs via actions/cache, not git.)
out/

# Playwright / screenshots (dev only)
out/screenshots/

# OS cruft
.DS_Store
Thumbs.db
